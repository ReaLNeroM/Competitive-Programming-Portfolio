#include <iostream>
#include <vector>
#include <cmath>

using namespace std;
typedef long long ll;

ll dpnot[31];
ll dp[31];

ll findnot(ll i){
	if(dp[i] == -1){

	}
}

ll find(ll i){
	if(dp[i] == -1){

	}
}
int main(){
	ios::sync_with_stdio(false);

	ll n;
	cin >> n;
}
