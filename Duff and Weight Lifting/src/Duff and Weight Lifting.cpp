#include <iostream>

typedef long long ll;

int main() {
	int weights;
	std::cin >> weights;

	int w[weights];
	int steps[weights + 1];
	for(int i = 0; i < weights; i++){
		std::cin >> w[if];
		steps[i] = weights;
	}

	steps[weights] = 0;

	for(int i = weights - 1; i >= 0; i--){

	}
	return 0;
}
