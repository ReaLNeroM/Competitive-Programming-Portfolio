#include <bits/stdc++.h>

typedef long long ll;

int main(){
	std::ios::sync_with_stdio(false);

	int n, q;
	std::cin >> n >> q;

	for(int i = 0; i < q; i++){
		int ind;
		std::cin >> ind;

		while(ind % 2 == 0){
			ll left = (ind - 2) / 2;
		}

		std::cout << ind << '\n';
	}
}
