#include <iostream>
#include <vector>
#include <cmath>

class CisaroMath {
	public:
		std::string solve(int a, int b, int n) {
			if(a < )
		}
};

int main(){
	int a, b, c;
	std::cin >> a >> b >> c;
	CisaroMath x;
	x.solve(a, b, c);
}
