#include <bits/stdc++.h>

typedef long long ll;

int main(){
	std::ios::sync_with_stdio(false);

	int n;
	std::cin >> n;

	std::priority_queue<int> pq;
	for(int i = 0; i < n; i++){
		int prob;
		pq.push(prob);
	}

}