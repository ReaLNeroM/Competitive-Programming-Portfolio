#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

const ll maxn = 505;
ll val[maxn];

int main(){
	ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);

	ll n;
	cin >> n;
	for(int i = 0; i < n; i++){

	}
}